{
  "name": "guide-proxy-server",
  "version": "0.1.0",
  "private": true,
  "description": "Simple HTML injection proxy for browser guide testing",
  "scripts": {
    "start": "node server.js",
    "validate:schema": "node -e \"require('./validate-guide').selfTest()\""
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "driver.js": "^1.6.0"
  }
}
